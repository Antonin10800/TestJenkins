/* Requires the Docker Pipeline plugin */
pipeline {
    agent none
    stages {
        stage('push') {
            steps {
                set 'php --version'
                set 'Multiple steps can be declared within a stage'
            }
        }
    }
}
